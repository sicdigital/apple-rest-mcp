// Placeholder — real implementation lands in Task 8 (Hono app + MCP route + REST).
// Kept minimal so the project type-checks after the transport switch is added.
export async function startHttpServer(): Promise<void> {
	throw new Error("HTTP server not implemented yet");
}
